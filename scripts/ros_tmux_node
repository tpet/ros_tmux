#!/bin/bash
set -e
trap "tmux wait-for -S $1" EXIT

ros_arg() {
    for arg in "${@:2}"; do
        if [[ $arg == "$1:="* ]]; then
            start=`expr \${#1} + 2`
            echo "${arg:${start}}"
        fi
    done
}

log() {
    echo -e [ros_tmux] `date "$date_fmt"`: "$@" >> "$log" 2>&1
}

date_fmt='+%Y-%m-%d %H:%M:%S.%3N'
log="$ROS_LOG_FILENAME"

log "ROS env:\n`env | grep ROS`"
log "Node command: ${@:2}"

"${@:2}" 2>&1 | tee -a "$log"
log "Node return code: $?"
